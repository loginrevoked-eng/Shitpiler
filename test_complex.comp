name = "Alice"
age = 25
score = 95.5

if (age >= 18) {
    builtin_print("Hello " + name + ", you are an adult!")
    builtin_print("Your age is: " + age)
}

if (score >= 90) {
    builtin_print("Excellent score!")
} otherwise {
    builtin_print("Keep practicing!")
}

# Test with a younger person
age = 15
name = "Bob"

if (age >= 18) {
    builtin_print(name + " is an adult")
} otherwise {
    builtin_print(name + " is not an adult yet")
}
